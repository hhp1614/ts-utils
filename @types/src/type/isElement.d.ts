/**
 * 判断是否为 DOM 元素
 * @param value 要判断的值
 * @example
 *
 * isElement(document.body);
 * // => true
 *
 * isElement('<body>');
 * // => false
 */
export declare function isElement(value: any): boolean;

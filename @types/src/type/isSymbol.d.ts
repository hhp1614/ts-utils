/**
 * 判断是否为 Symbol 对象
 * @param value 要判断的值
 * @example
 *
 * isSymbol(Symbol.iterator);
 * // => true
 *
 * isSymbol('abc');
 * // => false
 */
export declare function isSymbol(value: any): boolean;

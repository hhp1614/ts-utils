/**
 * 判断是否为类型数组
 * @param value 要判断的值
 * @example
 *
 * isTypedArray(new Uint8Array);
 * // => true
 *
 * isTypedArray([]);
 * // => false
 */
export declare function isTypedArray(value: any): boolean;

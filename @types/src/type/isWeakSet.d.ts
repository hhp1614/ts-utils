/**
 * 检查是否为 WeakSet 对象
 * @param value 要检查的值
 * @example
 *
 * isWeakSet(new WeakSet);
 * // => true
 *
 * isWeakSet(new Set);
 * // => false
 */
export declare function isWeakSet(value: any): boolean;
